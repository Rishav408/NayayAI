# ============================================================
# NyayaAI Backend — Production Requirements
# Python 3.11 (pinned via runtime.txt + PYTHON_VERSION env var)
# ============================================================

# Core Framework
flask==3.0.0
flask-cors==4.0.0

# LangChain 0.1.x ecosystem — pinned as a compatible set
# langchain 0.1.x requires langchain-core >=0.1.7, so 0.1.0 causes conflicts
langchain>=0.1.9,<0.2
langchain-core>=0.1.9,<0.2
langchain-community>=0.0.20,<0.1

# Google Gemini integration (must stay on 1.x — 2.x requires langchain-core 0.3+)
langchain-google-genai>=1.0.1,<2.0
google-generativeai>=0.3.2,<0.5

# Search Tools
duckduckgo-search==5.1.0

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0
beautifulsoup4==4.12.2
lxml==4.9.3

# Data Validation
pydantic>=2.5.0,<3.0

# Environment Management
python-dotenv==1.0.0

# HTTP
requests==2.31.0
urllib3>=2.1.0,<3.0

# Logging
structlog==23.2.0

# Production WSGI Server
gunicorn==21.2.0
